{
  "name": "pawbandhan",
  "version": "1.0.0",
  "private": true,
  "description": "PawBandhan — API + web portals",
  "scripts": {
    "start": "node backend_api/server.js",
    "build": "cd backend_api && npm install --omit=dev",
    "vercel-build": "cd backend_api && npm install --omit=dev && cd .. && npm install --omit=dev && node scripts/vercel-prepare.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "serverless-http": "^3.2.0"
  }
}
